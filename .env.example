# Example environment configuration
# Copy this file to .env and fill in real secrets (never commit .env)

# Core runtime
NODE_ENV=development
PORT=3000
# IMPORTANT: Change this to a strong random string in real environments
JWT_SECRET=change_me_dev_secret_please
ACCESS_TTL_SEC=900
REFRESH_TTL_SEC=86400

# Plaid (T-022) - required for real aggregator flows
PLAID_CLIENT_ID=your_sandbox_client_id
PLAID_SECRET=your_sandbox_secret
# Options: sandbox | development | production
PLAID_ENV=sandbox

# Notes:
# - config:check will fail in production if JWT_SECRET is left at default length/placeholder.
# - PLAID_* vars are optional until you exercise link/token exchange; tests use mock adapter.
